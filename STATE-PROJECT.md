---
title: "Logos Iris — State of Project"
date: 2026-09-10
# ════════════════════════════════════════════════════
# MÁQUINA — lido por /logos e hooks. Não renomear campos.
# ════════════════════════════════════════════════════
fase_atual: "7"
etapa_atual: "Build Backend — wave 1 (retomada 2026-09-10, build inline)"
# produto_tipo: saas-premium | dashboard | landing | mvp
produto_tipo: "saas-premium"
proximo_passo: "Wave 1 backend: human-handoff ✅ (2026-09-10, inline, 29 testes, tsc+eslint limpos). Faltam cost-observability-v1 e persona-agendamento (dep. migration tenants.timezone, task 8.1). Depois: /code-review da wave inteira → painel-cliente-v1 (fase 8) → Fase 10 Validate. Dispatch de worker registrado indisponível nesta sessão (ver Bloqueios) — seguir inline ou reabrir sessão na raiz do monorepo."
# fases_skipped: preenchido pelo init conforme produto_tipo
fases_skipped: []
# gates: ex { fase_1: pass, fase_2: pass }  ·  features: ex { auth: { spec: approved, built: true } }
gates: { fase_1: "pass", fase_2: "pass", fase_3: "pass", fase_4: "pass", fase_5: "pass", fase_6: "pass", fase_7: "pass", fase_8: "pass", fase_9: "pass" }
features: {
  whatsapp-gateway: { spec: "approved", wave: 0, built: true, reviewed: true },
  tenant-router-queue: { spec: "approved", wave: 0, built: true, reviewed: true },
  message-debouncer: { spec: "approved", wave: 0, built: true, reviewed: true },
  model-gateway-v1: { spec: "approved", wave: 0, built: true, reviewed: true },
  conversation-engine-v1: { spec: "approved", wave: 0, built: true, reviewed: true },
  persona-atendimento: { spec: "approved", wave: 1, built: true, reviewed: true },
  persona-agendamento: { spec: "approved", wave: 1, built: false },
  knowledge-base-v1: { spec: "approved", wave: 1, built: true, reviewed: true },
  contact-memory: { spec: "approved", wave: 1, built: true, reviewed: true },
  human-handoff: { spec: "approved", wave: 1, built: true, reviewed: false },
  painel-cliente-v1: { spec: "approved", wave: 1, built: false },
  feature-gating-v1: { spec: "approved", wave: 1, built: true, reviewed: true },
  cost-observability-v1: { spec: "approved", wave: 1, built: false }
}
# overrides: ex [{ gate: "sdd-auth", motivo: "...", data: "..." }]
overrides: []
# ════════════════════════════════════════════════════
# HUMANO
# ════════════════════════════════════════════════════
# status: 🟢 Ativo | 🟡 Novo/Pausa | 🔴 Bloqueado
status: "🟢 Ativo"
tags: [status, roadmap]
---

# ESTADO — Logos Iris

> Driver único do projeto. A skill `/logos` lê o frontmatter para rotear
> e atualiza este arquivo ao fim de cada etapa. Editar à mão só o corpo.

## Em Andamento
- [ ] Fase 7: Build Backend — wave 1 (8 features, docs/tasks.md linhas 34-56). Grafo de dependência: `persona-atendimento` (7.1-7.2, sem dep de wave 1) bloqueia `persona-agendamento` (8.1-8.3, dep 7.1) e `human-handoff` (11.1-11.3, dep 7.1). `knowledge-base-v1` (9.1-9.2), `contact-memory` (10.1-10.2), `feature-gating-v1` (13.1), `cost-observability-v1` (14.1) são independentes entre si e de `persona-atendimento`. `painel-cliente-v1` (12.1-12.3) é FASE 8 (frontend), não fase 7, e sua ação 12.3 depende de `human-handoff` — fica pra depois.
  - Rodada 1: ✅ COMPLETO. `persona-atendimento` (worker a5da98badf747eb35 — 14/14 testes, migration 0020 wrapper SECURITY DEFINER) + `knowledge-base-v1` (worker a7ad2378cb00a24f4 — 30/30 testes). **Colisão de migration:** os 2 workers pegaram `0020` em paralelo sem se ver — knowledge-base-v1 renumerada pra `0021` antes do merge. **Achado do orquestrador pré-merge em knowledge-base-v1:** `SupabaseKnowledgeEntryRepository.setStatus` fazia UPDATE só por `id`, sem `.eq("tenant_id",...)` (inconsistente com `upsertDraft` e com o princípio de defesa em profundidade documentado no próprio arquivo) — não explorável hoje (todo `id` chega de `findByTenant` já tenant-scoped) mas corrigido antes do merge (parâmetro `tenantId` obrigatório + teste de spy dedicado). Merge `--no-ff` limpo nos 2. Pós-merge: `tsc --noEmit` limpo, `npm run build` limpo, suite 156/160 (mesmas 4 falhas pré-existentes). `registry.json`/frontmatter: ambos `built: true, reviewed: true`.
  - Rodada 2: ✅ COMPLETO (ambas caíram no bug do worktree/repo aninhado na 1ª tentativa, abortaram com segurança via auto-checagem, redespachadas). `feature-gating-v1` (worker a972c0ca288991ce4 — 30/30 testes, `CheckFeatureGate` default-deny, admin bypass ADR-030, `FollowUpService` como referência, sem migration nova). `contact-memory` (worker adae536591d40ab00 — 10/10 testes + pgTAP 07 novo, migration 0022: geração de resumo + purge diário LGPD/ADR-026 + cascata de esquecimento em `contacts.deleted_at` + fila pgmq + Edge Function `contact-memory-summarizer-worker`, reusa token Vault de 0017/0019 sem nunca ecoar/logar; estende `contact-memory.repository.ts` do persona-atendimento sem duplicar). 2 Sync Requests não-bloqueantes (task_type dedicado de sumarização no model_registry; tier_do_plano real no generator — ambas hardcoded como stopgap documentado). Merge `--no-ff` limpo nos 2. Pós-merge: `tsc --noEmit` limpo, suite 196/205 (mesmas 4 falhas pré-existentes). `registry.json`/frontmatter: ambos `built: true, reviewed: true`.
  - Rodada 3 (2026-08-13): `cost-observability-v1` + `persona-agendamento` despachada em paralelo — **nunca aterrissou** (sessão morreu; registry confirmou `built: false` na retomada de 2026-09-10).
  - Retomada 2026-09-10 (build INLINE, sem worker): `human-handoff` ✅ COMPLETO (ver Concluído). Faltam `cost-observability-v1` (14.1, independente) e `persona-agendamento` (8.1-8.3, dep. migration `tenants.timezone` + ALTO-2 Vault antes do path outbound). Depois: `/code-review` da wave inteira.
- [ ] Fase 7: Build Backend — wave 0. `/logos parallel` dispatchado 2026-08-04.
  - `model-gateway-v1`: ✅ COMPLETO. Worker `backend-engineer` (16/16 testes) → merge `--no-ff` em master (sem conflito) → `npm install` + suite re-rodada 16/16 verde → `spec-reviewer` (agentId a8bc8d284cb7f4c65) auditou independente (rodou testes e `tsc --noEmit` de novo, não confiou no self-report): **"Merge liberado"**, zero drift spec/contrato/implementação, camadas C→S→R respeitadas, zero `any`. Único gap não-bloqueante: `ModelRegistryQueryError` (falha de driver, 502) sem teste direto — aceito, registrado abaixo. `registry.json`: `built: true`, `reviewed: true`.
  - `whatsapp-gateway`: ✅ BUILD completo, review pendente. Worker `backend-engineer` (agentId a00939d40c4f08cd3) entregou webhook receiver + Evolution/Cloud API/OpenWA adapters + dedup via `webhook_inbox`/pgmq. **Descoberta crítica pré-merge:** as correções da Fase 6 (migration 0014 — grants service_role, índices FK, RLS soft-delete, updated_at triggers) tinham sido só verificadas ao vivo no DB, nunca commitadas — `isolation: "worktree"` clona a partir do último commit, não do working tree, então o worktree do whatsapp-gateway (e os outros 2 da wave) nunca viu esses fixes. Commitado antes do merge (789b8bb). Merge `--no-ff` (1e25559) com 3 conflitos reais: `package.json` (mantido HEAD, superset mais novo), `package-lock.json` (mantido HEAD, `npm install` confirmou "up to date"), `service-client.ts` (add/add — worker criou `createServiceClient()`/`IrisSupabaseClient` com schema pinned, paralelo ao `getServiceRoleClient()` já existente com `.schema()` chaining; resolvido preservando as duas funções, já que 6 repositories do gateway dependem do padrão schema-pinned). `01_rls_tenant_isolation.sql` mergeou limpo (assertions 1-13 de master, #14 nova do worker). Corrigido `any` em `IrisSupabaseClient` (violava regra global TS) gerando `database.types.ts` real via Supabase CLI (MCP `generate_typescript_types` não aceita schema param, default só `public`). Pós-merge: `npm install` limpo, `npm run test` 34/34 + 1 todo (era 23/23), `tsc --noEmit` sem erros. Suite pgTAP `01_rls_tenant_isolation.sql` re-rodada ao vivo contra `nqubjiosnlaatxxamiut` (via temp-table log, já que `execute_sql` só retorna o resultado do último statement) — **15/15 `ok`, zero `not ok`**. `spec-reviewer` (agentId a77e6f35b141474e8) auditou independente: rodou `npm run test` (34/34+1 todo), `tsc --noEmit` (zero erros), `eslint` nos 15 arquivos da feature (zero issues), grep de `any` no diff completo incl. `database.types.ts` (zero), grep de marcadores de conflito não resolvidos (zero). **"Merge liberado."** 3 achados não-bloqueantes registrados em Sync Requests Pendentes abaixo (código morto `updateSessionStatus`, webhook secret fail-open + vars fora do `.env.example`, `HumanHandoffService` não chamado — gateway grava direto em `handoff_events`). `registry.json`/frontmatter: `built: true`, `reviewed: true`. ✅ COMPLETO.
  - `conversation-engine-v1`: ✅ COMPLETO (build+review). Worker entregou só `compilePrompt` (camadas 1-2 do prompt 4-camadas, ADR-027) — SEM Controller/route.ts e SEM consumir `model-gateway.service.ts`. `spec-reviewer` (a52d9c78881de9af3) confirmou linha a linha contra `docs/specs/conversation-engine-v1.md`: seção "Restrições Técnicas" diz literalmente "nenhum HTTP — função interna" e seção "Fora de Escopo" exclui chamada real ao ModelGateway nesta v1 — os 2 desvios são spec-driven, não corte de escopo disfarçado. Verificação independente: 7/7 testes, `tsc`/`eslint` limpos, zero `any`, zero PII no prompt. **Merge liberado.** Merge `--no-ff` + suite 23/23 verde. `registry.json` e frontmatter `features:` sincronizados: `built: true`, `reviewed: true`.
  - `tenant-router-queue`: ✅ BUILD completo, ⚠️ REVIEW com blockers. Worker anterior (ab17151b8fdf5f505) morreu por limite de sessão 2026-08-04 (0 código). Redispatch (agentId a245b153a49941b28) após reset entregou: 3 funções SECURITY DEFINER em `iris` (0016 — `router_route_inbound_message` sob advisory lock, `router_consume/delete_whatsapp_inbound` sobre pgmq), `TenantRouterService`/factory/errors, `conversation-routing.repository.ts`, `tenant-lookup.repository.ts`, schema Zod, Edge Function `tenant-router-worker`, pgTAP 05. Merge `--no-ff` limpo (11 arquivos, 722 ins.). Verificação independente: `tsc --noEmit` zero erros · `vitest run` 44/44 · **migration 0016 aplicada ao DB live + pgTAP 05 re-rodada ao vivo (temp-table log, `\set`→literais, cleanup net-zero): 9/9 `ok`** (advisory lock por conversa serializa; conversas distintas paralelizam — 2 locks; find-or-create idempotente; RLS isola `conversation_state` entre tenants; queue `whatsapp_inbound` confirmada existente). `spec-reviewer` (agentId aaff386f77f88e4f5) **"Corrigir antes de prosseguir"** — 3 achados materiais (2 CRÍTICOS + 1 ALTO, ver Sync Requests). Os 3 desvios documentados na migration (lock key = tenant+contact; persona = personas_ativas[1]; debounce 8s/TTL 1d) julgados ACEITÁVEIS pelo reviewer. `registry.json`/frontmatter: `built: true, reviewed: false`. **Aguardando decisão de Vinicius (gate Sync Requests) antes de corrigir.** `message-debouncer` segue bloqueada.
  - Housekeeping pendente: 3 worktrees de workers já mergeados (a00939d40c4f08cd3, a3302aec392112a21, af0691d8178d52f54) ainda em `.claude/worktrees/` — todos confirmados ancestrais de master, seguros pra `git worktree remove`. Prune bloqueado pelo classifier de auto-mode nesta sessão; limpar manualmente ou em sessão interativa.
  - Nota de cobertura (não bloqueante, não é Sync Request): `ModelRegistryQueryError` em `model-registry.repository.ts` (path de falha do client Supabase) sem teste — mockar client Supabase pra cobertura 4/4 se algum dia doer; achado do `spec-reviewer`, não corrigido agora por não ser gap de spec/contrato.

## Concluído
- [2026-09-10] **`human-handoff` (wave 1) — BUILD completo, review pendente (fica pro /code-review da wave).** Construído INLINE (sem worker — agentes registrados indisponíveis nesta sessão). `HumanHandoffService` como casa canônica dos 5 gatilhos + resume nunca silencioso: `pauseConversation` (botao_painel/comando_chat), `resumeConversation` (pausa longa → `aguardando_confirmacao` sem `confirmado_pelo_dono`; limiar `HANDOFF_LONG_PAUSE_HOURS` default 24, request-time, sem cron conforme spec), `pauseFromOwnerMessage` (from_me_detectado, auto-pausa `HANDOFF_AUTO_PAUSE_HOURS` default 2), `escalateByCustomerRequest` (pedido_cliente + dossiê de 3 linhas derivado só de contact_memory cifrado + metadados estruturais, truncado — LGPD), `escalateByLowConfidence` (baixa_confianca). Sem migration nova (handoff_events/conversations da 0005 já têm `resolvido_em`/`retomada_confirmada`/`pausada_ate`), sem lib nova. Repo: `HandoffRepository` intocada (fakes de whatsapp-gateway/persona-atendimento dependem do shape antigo); superfície nova em interface SEPARADA `HumanHandoffRepository`, `SupabaseHandoffRepository` implementa as duas. Rotas `POST /api/conversations/[id]/(pause|resume)` (Controller thin, tenant do JWT, nunca do client). **Cross-feature:** `whatsapp-gateway.service.ts` religado — `from_me_detectado` + `comando_chat` (#eu/#iris) agora passam pelo `HumanHandoffService` (consolidação que a Sync Request #3 de 2026-08-04 pedia); 4 asserts do teste do gateway ajustados + 2 testes novos de comando. `.env.example` + as 2 vars novas. Verificação: `tsc --noEmit` limpo · `eslint` limpo nos 12 arquivos · suite 225/234 (as MESMAS 4 falhas pré-existentes: 3 `pages/*.tsx` parse rolldown + 1 flake timing debouncer — baseline reconfirmado antes do build era 196/205). +29 testes (`human-handoff.test.ts`: 1 por gatilho + resume-com/sem-confirmação + isolamento tenant A/B + Zod + estado encerrado + console limpo). Checklist security 7/7. Frontmatter + `registry.json`: `built: true, reviewed: false`. **Não pushado** (repo externo — Vinicius faz o push).
- [2026-08-13] **Fase 9 — Build Integrações. GATE FECHADO (pass).** Review de segurança dedicado (`revisor-codigo`) sobre o hardening do `whatsapp-gateway` (commit `865d721`): zero findings críticos, mas 2 ALTOS. **ALTO-1** (rate limit contornável — `clientIdentifier()` confiava no hop mais à esquerda de `X-Forwarded-For`, client-controlled; atacante rotacionando o header nunca era limitado) **corrigido nesta sessão** (commit `c65893b`): prioriza `x-vercel-forwarded-for` (header de plataforma, stack Vercel padrão) e usa o hop mais à direita como fallback; extraído para `client-identifier.ts` testável (route.ts do Next.js não pode exportar funções extras) com teste de regressão dedicado. **ALTO-2** (`credentials_ref` usado como credencial crua em vez de resolvido via Supabase Vault nos 3 adapters — viola ARCHITECTURE.md §1.2 e a migration 0002; `send()`/`pareamento()` não têm caller em produção hoje, só testes) **NÃO bloqueia o gate** por decisão do próprio reviewer, mas é **obrigatório antes de qualquer feature da wave 1 ligar o path outbound** (ver Sync Requests). MÉDIO-1/2 e BAIXO-1/2 registrados como follow-up não-bloqueante (Sync Requests). Suite pós-fix: 111/114 (4 falhas pré-existentes e não-relacionadas, confirmadas idênticas às da recuperação anterior). `tsc --noEmit` limpo. gates.fase_9: pass. Avançando para Fase 10: Validate.
- [2026-08-13] `integration-engineer` (fase 9) — hardening `whatsapp-gateway`: clients HTTP tipados p/ Evolution/OpenWA/Cloud API (retry backoff+jitter, timeout `AbortController`, erros por categoria) via `http-retry.ts` compartilhado; 3 adapters religados (antes: `fetch()` cru); webhook fail-open→fail-closed + HMAC nativo Meta (`X-Hub-Signature-256`) + segredo compartilhado como fallback; rate limiting básico (janela deslizante em memória); `.env.example` completo; testes dos 3 cenários (sucesso/timeout/malformado) nos 3 clients + adapters + auth + rate limiter. **Descoberta estrutural (não é bug do worker):** `isolation:"worktree"` resolveu para um `.git` ANINHADO em `04-Projetos/01-Projetos/Logos Iris/.git` (repo próprio, remote `github.com/ViniciusGrossi/Logos-Iris`, história parada em `34b4907`/fase-8-gate-pass) em vez do `.git` do monorepo (`Logos Tech/.git`, branch `master`) — worker trabalhou legitimamente, mas sobre uma base sem os commits `91537f2`/`883508d`. Detectado ao checar branch/merge-base pós-conclusão. Recuperação manual: 5 commits do worker inspecionados (`git diff --name-status`), arquivos-base comparados entre `main` (base do worker) e `master` atual — idênticos em todos exceto `evolution.client.ts`/`.errors.ts` (add/add: master tinha versão morta/não-usada, worker tinha versão superior com taxonomia de erro mais granular e reuso de `http-retry.ts` — mantida a do worker) e `.env.example` (worker não sabia que master já tinha um `.env.example` com vars do gateway — versão do worker é superset melhor documentado, mantida). Arquivos copiados manualmente pro checkout do monorepo, `npm install` + `tsc --noEmit` + suite rodados no monorepo (não no worktree) pra validar o que de fato vai pro `master`. Achado extra: `.env.example` nunca tinha sido de fato commitado no monorepo — `.gitignore` tinha `.env*` sem exceção, bloqueando silenciosamente; corrigido (`!.env.example` adicionado). `package-lock.json` também estava desincronizado de `package.json` no monorepo (pré-existente, não causado por hoje) — corrigido via `npm install`. Worktree removido (`git worktree remove --force` + `core.longpaths=true` no repo aninhado, mesmo fix já usado no monorepo) após extrair o conteúdo. Suite pós-recuperação: 106/109 passando (4 falhas pré-existentes e não-relacionadas: 3 `pages/*.tsx` com erro de parse JSX do rolldown/vite, 1 flake de timing em `message-debouncer` — confirmadas idênticas ao que o worker já reportou antes de qualquer mudança). Commit `865d721`. Review de segurança dedicado disparado em seguida (ver Workers Ativos) — gate fase 9 só fecha depois dele.
- [2026-08-13] `prompt-engineer` (fase 9) — `docs/prompts/` criado: `index.md` + `nucleo.md` + 4 `persona-*.md` (atendimento/vendas/agendamento/sdr), cada um com modelo (`claude-*`), temperatura justificada, few-shot, grounding anchor, confirmação de ausência de CoT. Não moveu código (ADR-027 mantém `NUCLEO`/`PERSONA_TEMPLATES` em `conversation-engine.service.ts` como fonte de verdade) — documentação espelha o texto verbatim. Achado registrado com honestidade: `CompiledPromptV1` NÃO concatena núcleo+persona nesta v1 (campos separados, precedência por tipagem, confirmado via teste adversarial `conversation-engine-v1.test.ts:34-48`) — o brief presumia concatenação, o worker corrigiu a premissa em vez de documentar algo inexistente. Skill `/prompt-master` indisponível no ambiente — checklist aplicado manualmente. Zero Sync Requests. Commit `883508d`. `docs/prompts/` verificado no disco, escopo respeitado (só esse diretório tocado).
- [2026-08-07] `tenant-router-queue` — correção pós-review fechada, gate humano (Sync Requests) liberado. Ver detalhe em Sync Requests Pendentes (bloco 2026-08-05, RESOLVIDO) e incidente de segurança em Lições. `features.tenant-router-queue: { built: true, reviewed: true }`. **Não commitado no git** — ver Bloqueios.
- [2026-08-04] Fase 6 — Build Banco. Worker `dba` retomado (mesmo agent, `SendMessage` — não respawn) após reconexão do MCP Supabase: 13 migrations + seed aplicadas no projeto real (`nqubjiosnlaatxxamiut`, schema `iris`+`iris_private`), pgTAP 32/32 verde. Review adversarial `@revisor-codigo` achou 1 CRÍTICO real (`service_role` sem nenhum GRANT de tabela em `iris` — `BYPASSRLS` não substitui `GRANT`; pgTAP tinha passado porque rodou como owner, mascarando o gap) + 4 baratos (3 índices FK faltando, soft-delete vazando em 3 policies de SELECT, `updated_at`+trigger faltando em 3 tabelas, `anon` com USAGE sobrando). Devolvido ao mesmo worker dba via SendMessage (protocolo: continuar agent existente). Migration `0014` corrigiu os 5 + nova suite pgTAP `04_service_role_grants.sql` provando o fix (INSERT/SELECT como `service_role` puro em `messages`/`webhook_inbox`). Suite final: 36/36, zero `not ok`. Advisors sem regressão nova. 1 gap arquitetural aceito e registrado como Sync Request (RLS ausente nas 26 partições filhas de `messages`/`model_usage_log` — `ensure_monthly_partition()` não herda RLS; mitigado por `iris` não estar em Exposed Schemas). 1 Sync Request encaminhada pra fase 7 (JSONB `conversation_state.state`/`conversation_signals.payload` fora do plano pgcrypto — engine não pode gravar PII bruta neles). gates.fase_6: pass. Avançando para Fase 7: Build Backend.
- [2026-07-12] Fase 5 — Decomposition. `docs/tasks.md`: 22 tracer bullets em 5 waves (0 Fundação · 1 Atendimento+Agendamento vendável cedo · 2 Vendas+SDR · 3 camada artesanal · 4 Admin+Billing), granularidade ≤4h, dependências mapeadas, HITL/AFK marcados. `/to-issue` e `/gsd-plan-phase` desviados por incompatibilidade de skill (mesma classe de bug de fase-01/02, ver Lições) — decomposição feita manualmente. 13 specs completas draftadas em batch (2 workers paralelos) pra Wave 0+1 — template `spec-feature-v2.md`, TDD oracle (Given/When/Then por critério de aceite). `specs/registry.json` criado: 13 features `approved` (wave 0+1) + 8 `draft` sem spec ainda (wave 2-4, spec própria no início de cada wave). `/grill-me` resolveu 4 branches: (1) baixa_confiança (HumanHandoff ★) = tool-call de escalonamento da própria LLM, sem score numérico; (2) endpoint QR-pareamento WhatsApp adiado pra wave 4 (painel-admin-completo), não bloqueia build atual; (3) migration `tenants.timezone` aprovada (gap ARCHITECTURE.md, task 8.1); (4) escopo do gate = Wave 0+1 como MVP prático, Waves 2-4 com spec perto do build (mitiga risco do PRD "construir demais antes de feedback real"). Gap de paginação em `GET /api/admin/model-registry` aceito como está (tabela pequena/global). 🔒 Specs Batch apresentado (índice 1 linha/feature) e aprovado por Vinicius. gates.fase_5: pass. Avançando para Fase 6: Build Banco.
- [2026-07-11] Fase 4 — Design. Protótipo real em Next.js (Inbox, Dashboard, Admin Tenants) via `/frontend-design:frontend-design` (direção "Manhã Clara — restrained", registrada em design-system.md). Handshake Visual aprovado por Vinicius. Pós-aprovação: polish de animação nas 3 telas (stagger, hover-lift, pane-transition, pulsing unread dot, usage-bar fill) com vocabulário de motion do design-system (`--duration-base`, `--ease-out-exp`, sem bounce). Auditoria pós-aprovação: taste-skill fora de escopo para este artefato (rubrica é landing/portfolio, não dashboard/data-table/chat-UI — aplicados só os princípios genéricos: consistência de cor/forma, motion motivado, estados de feedback) + `/polish` 22 pontos rodado, 1 finding (cores hardcoded `bg-neutral-100`/Tailwind palette em `status-dot.tsx` e `message-bubble.tsx`, bypassando `@theme`) corrigido na raiz: novos tokens `--status-ok/idle/off` em globals.css (light+dark), dissociados do espectro de persona conforme design-system.md. Screenshots regerados (1440/375) em `docs/design-refs/prototype/` e semeados em `docs/design-refs/seed/` (baseline fase 8). `npx tsc --noEmit` limpo. Gate fase_4 validado 7/7 (tokens, @theme, protótipo navegável, screenshots, handshake, auditoria, direção registrada). gates.fase_4: pass. Avançando para Fase 5: Decomposition.
- [2026-07-09] Fase 3 — Arquitetura. @arquiteto-sistemas entregou proposta completa (modelagem, 7 ADRs, pgcrypto, 6 riscos). `/grill-me` travou 2 decisões: (1) Edge agora, worker quando doer (ADR-031); (2) Provedor chinês SÓ no tier básico + minimização PII + golden set qualidade (ADR-025). docs/ARCHITECTURE.md escrito com 7 ADRs, modelagem completa, plano pgcrypto, Decisões Desafiadas. ADRs 025-031 registradas no vault. Gate fase_3 validado 7/7 contra disco (ARCHITECTURE.md completo, stack confirmada, 7 ADRs, C→S→R mapeado, multi-tenant definido, Decisões Desafiadas presente, plano pgcrypto definido). gates.fase_3: pass. Avançando para Fase 4: Design.
- [2026-07-03] Projeto inicializado com planejamento-fable.md via Fable 5
- [2026-07-08] Fase 1 — Ideia. `/grill-me` sobre planejamento-fable.md resolveu 3 decisões: (1) público-alvo amplo/multi-vertical por decisão de GTM consciente (sem verba de tráfego pago = volume naturalmente baixo, sem precisar de piloto restrito a 1 vertical — dado real do Agora: advocacia é o segmento mais quente, 22% conversão vs ≤7%); (2) job principal = captura de receita (não perder venda por demora), delegação/alívio é secundário; (3) camada artesanal manual aceita por ora, onboarding automatizado vira hipótese de saída de escala. docs/ideia.md criado. Gate fase_1 PASS.
- [2026-07-08] Fase 2 — PRD. docs/PRD.md (Problem/Solution/42 User Stories/11 módulos/Testing nos 3 ★ ModelGateway·ConversationEngine·HumanHandoff/Out of Scope/Métricas de Sucesso) + specs/product.schema.json (12 entidades) + specs/api.contracts.ts (compila tsc --strict). 3 decisões travadas: tenant:whatsapp 1:1, camada artesanal como entidade própria (versionamento separado do enriquecimento do cliente), painel admin com acesso hardcoded (sem tabela de roles). @spec-reviewer achou 3 gaps bloqueantes (Agendamento e Follow-up sem entidade/endpoint, RLS faltando em handoff_events) — corrigidos antes do lock. Gate f2 validado via `/logos` v8 (`scripts/validate.py`, self-contido) após Vinicius consertar o path morto do logos-cli. PRD Lock aprovado por Vinicius. Gate fase_2 PASS.

## Specs
| Spec | Status | Atualização |
|---|---|---|
| docs/ideia.md | pronto | 2026-07-08 |
| docs/PRD.md | pronto (travado) | 2026-07-08 |
| specs/product.schema.json | pronto (travado) | 2026-07-08 |
| specs/api.contracts.ts | pronto (travado) | 2026-07-08 |
| docs/ARCHITECTURE.md | pronto | 2026-07-09 |
| docs/tasks.md | pronto | 2026-07-12 |
| specs/registry.json | pronto (13 approved, 8 draft) | 2026-07-12 |
| docs/design-system.md | pronto | 2026-07-11 |

## Workers Ativos
> Preenchido por /logos ao spawnar agents (máx 2 simultâneos). Limpo ao concluir.

- — (nenhum. Rodada 3/2026-08-13 de `cost-observability-v1` + `persona-agendamento` nunca aterrissou — sessão morreu, registry confirma `built: false`. Slots limpos na retomada 2026-09-10.)

## Sync Requests Pendentes
> Spec Sync Requests acumulados na wave atual. Apresentados em batch.

- [2026-09-10] **human-handoff — 3 desvios não-bloqueantes (registrados, decisão no batch da wave):**
  1. `persona-atendimento.service.ts::escalateToHuman` (já buildada/revisada, módulo ★) ainda chama `handoffRepo.pauseForHandoff({gatilho:'baixa_confianca'})` direto em vez de delegar ao novo `HumanHandoffService.escalateByLowConfidence`. Ambos produzem `handoff_events` idêntico — não consolidado agora pra não re-tocar um ★ revisado no mesmo passe. Consolidar quando o loop de tools da engine existir.
  2. `escalateByCustomerRequest` (pedido_cliente) e `escalateByLowConfidence` (baixa_confianca) do `HumanHandoffService` não têm caller vivo — `conversation-engine-v1` não executa tools ("Fora de Escopo" da spec dela). Mesma situação já ACEITA pelo spec-reviewer pro `escalateToHuman` de persona-atendimento. Ligar quando a engine ganhar tool loop.
  3. Dossiê de `pedido_cliente`: contrato HTTP `PauseConversation` retorna exatamente `{status:"pausada"}`; o dossiê sai só pelo método INTERNO `escalateByCustomerRequest` (`{status, dossie}`), consumido pela tool da engine, não pela rota. Sem desvio de contrato. Se algum dia o dossiê precisar persistir/aparecer no painel → migration (coluna em `handoff_events`) + escopo de `painel-cliente-v1`.
- [2026-09-10] ALTO-2 (Vault) — Vinicius aprovou em 2026-09-10 dobrar o fix na wave 1, MAS a resolução de `credentials_ref` via `vault.decrypted_secrets` exige função `SECURITY DEFINER` nova (migration, território DBA) — NÃO entrou no passe de `human-handoff` (que não liga o path outbound; `#iris`/from_me só mudam status, não enviam mensagem). Fica pro passe dedicado antes de `persona-agendamento` (job de confirmação de véspera ENVIA mensagem via adapter).

- [2026-08-13] **ALTO-2 (review de segurança fase 9, obrigatório antes de ligar o path outbound do whatsapp-gateway):** `evolution.adapter.ts:99,122`, `openwa.adapter.ts:78,97`, `cloud-api.adapter.ts:82` passam `whatsapp_connections.credentials_ref` direto como `apikey`/`Authorization: Bearer` — mas essa coluna é uma REFERÊNCIA ao Supabase Vault (`ARCHITECTURE.md §1.2`, migration `0002:161`, seed grava `'vault:iris/bella-estetica/evolution'`), nunca o segredo em claro. Não há resolução de Vault em `src/` (grep vazio). Hoje inócuo porque `send()`/`pareamento()` não têm caller em produção (só testes) — mas qualquer feature de wave 1 que ligue o envio de mensagens vai autenticar com a string literal `vault:...` (quebrado) ou exigir colar o token real em claro na coluna (viola o schema, mesmo raio de explosão do incidente de 2026-08-07). Fix: resolver `credentials_ref` via `vault.decrypted_secrets` no Repository antes de entregar ao client. Cobrar antes de qualquer feature que chame `send()`.
- [2026-08-13] Achados MÉDIO/BAIXO não-bloqueantes do review de segurança fase 9 (registrados, não corrigidos — baixo valor ou sem caller em produção ainda): (1) `route.ts` lê o corpo do webhook via `req.text()` sem limite de tamanho antes do HMAC — mitigado pelo teto de 4.5MB da Vercel, ilimitado em self-host; considerar rejeitar via `Content-Length` acima de ~1MB; (2) `EvolutionClientError`/`OpenWaClientError`/`CloudApiClientError` não estendem `WhatsAppGatewayError` — inócuo enquanto `send()` não tem caller, mas quando um Controller chamar `send()` os erros tipados caem no catch genérico (500) e perdem a categoria; corrigir junto com o ALTO-2; (3) nenhum alarme runtime se `WHATSAPP_WEBHOOK_ALLOW_INSECURE_DEV=true` for setado em produção (decisão de não usar `NODE_ENV` já justificada em comentário) — `console.warn` no boot seria barato; (4) endpoint `GET` de handshake de verificação da Meta (`hub.challenge`) não implementado — a Cloud API real exige isso pra concluir a configuração do webhook; não estava nos 4 gaps do brief de hardening, precisa de decisão de produto (qual var carrega o verify token) antes de implementar.
- Dashboard Supabase: adicionar schema `iris` em Settings → API → Exposed Schemas (ação manual fora do write-scope do worker dba, necessária antes de `supabase-js .schema('iris')` funcionar em Fase 7+)
- [2026-08-04] RLS ausente nas 26 tabelas-partição filhas de `iris.messages`/`iris.model_usage_log` (13 cada, `_2026_08`→`_2027_08`): `iris_private.ensure_monthly_partition()` (0006/0012) cria a partição via `CREATE TABLE ... PARTITION OF` mas não roda `ENABLE ROW LEVEL SECURITY`/`FORCE ROW LEVEL SECURITY` nela — só a tabela-mãe particionada tem RLS+policies (confirmado via `pg_class.relrowsecurity`). Mitigado hoje por `iris` não estar em Exposed Schemas (sem acesso PostgREST), mas é gap de defesa em profundidade: acesso direto via SQL/service role endereçando a partição pelo nome ignora RLS. Decisão pendente: patchar `ensure_monthly_partition()` pra herdar RLS nas partições novas (afeta 0006 e 0012 simultaneamente) — não corrigido unilateralmente pelo worker dba (fora do mandato "executar, não redesenhar").
- [2026-08-04] Fase 7 (backend/engine): `iris.conversation_state.state` e `iris.conversation_signals.payload` são JSONB fora do plano pgcrypto — se o engine gravar slots com PII bruta (nome, telefone, CPF extraído de conversa) isso persiste em plaintext. Achado do `@revisor-codigo` na auditoria da fase 6 (schema não muda agora — decisão de comportamento de aplicação). Cobrar no build da engine: nunca persistir PII bruta nesses campos; se necessário, referenciar `contacts.*_enc`/`*_hash` em vez de copiar o dado.
- [2026-08-04] `whatsapp-gateway`: `DEFAULT_HANDOFF_PAUSE_HOURS = 2` (`src/repositories/handoff.repository.ts:13`) — worker fixou default conservador de "quanto tempo o bot fica pausado após handoff humano" sem valor especificado em spec/PRD. Decisão de produto pendente: 2h é o valor certo, ou deveria ser configurável por tenant (feature-gating-v1, wave 1)? Não bloqueia merge (comportamento seguro), mas fixar antes de wave 1 tocar em `human-handoff`.
- [2026-08-04] `whatsapp-gateway`: worker reportou "gap" de endpoint de QR-pareamento WhatsApp — já triado e adiado pra wave 4 (`painel-admin-completo`) via `/grill-me` na Fase 5 (ver Concluído, 2026-07-12). Não é Sync Request nova, worker não tinha visibilidade da decisão anterior (spec não referencia o adiamento explicitamente — considerar anotar isso na spec pra evitar redescoberta).
- [2026-08-04] RESOLVIDO: bug do `throws_ok(sql, code, description)` de 3 args (Postgres casa com overload `(text,text,text)`, 3º vira errmsg esperado em vez de descrição) reportado pelo worker whatsapp-gateway como achado novo nas assertions #6/#12/#13 de `01_rls_tenant_isolation.sql` — já era um problema pré-existente nessas mesmas linhas (não introduzido pelo worker), corrigido no commit 789b8bb (forma de 4 args) antes do merge. Suite completa re-verificada ao vivo pós-merge: 15/15 `ok`.

- [2026-08-04] `whatsapp-gateway` (achados do `spec-reviewer`, merge liberado — não-bloqueantes):
  1. `whatsapp-connection.repository.ts:6,27` implementa `updateSessionStatus()` mas nenhum adapter (evolution/openwa/cloud-api) chama em `catch` de falha de credencial/pareamento — Requisito Funcional 6 da spec não fica satisfeito na prática, hoje é código morto. Não bloqueia porque `send()`/`pareamento()` ainda não são invocados por ninguém (`tenant-router-queue` builda agora). Cobrar quando o consumidor de fila entrar: falha de credencial precisa atualizar `session_status`.
  2. `route.ts:17-21`: `verifyWebhookSecret` é fail-open se `WHATSAPP_WEBHOOK_SECRET` não estiver setada (comentado como "dev local" no código). `WHATSAPP_WEBHOOK_SECRET`, `EVOLUTION_API_BASE_URL`, `OPENWA_API_BASE_URL` não estão em `.env.example` — risco real de subir pra produção sem o secret e ninguém perceber pelo template. Além disso a spec pede validação "por assinatura/token do provedor" e a implementação usa segredo estático por servidor, não a assinatura HMAC nativa de cada provedor (ex. `X-Hub-Signature-256` da Meta). Ação: (a) adicionar as 3 vars em `.env.example`, (b) decidir se fail-open em dev é aceitável ou se precisa de flag explícita `NODE_ENV`, (c) avaliar assinatura nativa por provedor antes de produção real multi-tenant.
  3. `whatsapp-gateway.service.ts:55-75` grava direto em `conversations`/`handoff_events` via `HandoffRepository` em vez de acionar `HumanHandoffService` como a spec (`ARCHITECTURE.md` módulo 8) literalmente descreve — pragmático porque `human-handoff` ainda é wave 1 (`built:false`), mas a lógica de pausa vai duplicar quando `human-handoff` for construído. Consolidar nessa hora: `whatsapp-gateway` deve passar a chamar `HumanHandoffService` em vez de manter a escrita direta.
- [2026-08-04] RESOLVIDO 2026-08-07: Drift menor `wave: 1` → `wave: 0` no frontmatter de `tenant-router-queue.md`, corrigido junto com a correção pós-review.
- [2026-08-07] Novos, baixo valor/não-bloqueantes (registrados pelo 2º `spec-reviewer`, não corrigidos): (1) `tsconfig.json` exclui `supabase/functions` do type-check — nenhuma ferramenta type-checa as Edge Functions hoje (considerar `deno check` no gate de qualidade da fase 7/10); (2) `index.ts:55` faz cast `as QueueRow[]` sem validação Zod do retorno do RPC — dado gerado pela própria `0016`, risco baixo, mas é a única trust boundary do módulo sem Zod; (3) `vitest.config.ts` e `vitest.config.mts` coexistem (ambos uncommitted) — gera warning no `npm run test`, decidir qual remover.

- [2026-08-05] `tenant-router-queue` (achados do `spec-reviewer` aaff386f77f88e4f5 — veredicto "Corrigir antes de prosseguir"):
  1. **[CRÍTICO] Worker sem scheduling.** `supabase/functions/tenant-router-worker/index.ts` comenta "acionado por pg_cron" mas nenhum `cron.schedule`/`net.http_post`/trigger existe no repo (grep vazio). Sem invocação periódica, mensagens ficam paradas em pgmq `whatsapp_inbound` pra sempre — nenhum Req 1-5 se manifesta em prod. Padrão real já existe em `0013_partition_maintenance_cron.sql`. Fix: migration nova com `cron.schedule` + `net.http_post` pro endpoint da function. ⚠️ Decisão: corrigir agora (build) OU deferir scheduling+deploy da Edge Function pra Fase 12 (Deploy) e documentar como known-deferred na spec/ARCHITECTURE.
  2. **[CRÍTICO] Service TS é código morto.** `createTenantRouterService()`/`TenantRouterService` nunca importado fora do factory+teste. O caminho real de prod é o loop reimplementado no worker Deno, que NÃO usa Zod (`if(!number||!messageId)`), NÃO usa os erros tipados de `tenant-router.errors.ts`, sem teste próprio. Os 44 vitest testam código morto → não garantem o comportamento real. Fix: worker chama o Service real (resolver fronteira Next↔Deno) OU worker ganha teste próprio cobrindo os critérios de aceite. ⚠️ Decisão arquitetural: reusar vs manter duplicação + testar worker.
  3. **[ALTO] Tenant lookup não filtra tenant ativo.** `tenant-lookup.repository.ts:17-26` e `tenant-router-worker/index.ts:46-47` não filtram `status='ativo'`/`deleted_at is null` (`iris.tenants.status` existe desde 0002). Viola literalmente Req 5 + CA#2: tenant pausado/cancelado/soft-deletado ainda resolve `tenant_id` e roteia. Fix: `.eq("status","ativo").is("deleted_at",null)` no TS + query SQL do worker + 1 teste (tenant pausado → descartada). Recomendação: bug claro, corrigir independente da decisão de #1/#2.
  4. Texto da spec (`tenant-router-queue.md` linhas 25/49) ainda documenta `hashtext(conversation_id)` literal — desvio 1 é aceitável mas o texto deve refletir a chave real (`hashtext(tenant_id||':'||contact_id)`).
  - [2026-08-07] **RESOLVIDO.** Decisão de Vinicius: achado 1 → corrigir agora (migration cron), não deferir p/ Fase 12. Achado 2 → worker Deno passa a chamar o `TenantRouterService` real (resolver fronteira Next↔Deno via import map), não manter duplicação com teste próprio. Achados 3 e 4 corrigidos independente. Worker `backend-engineer` (abf12184e67e03fa7) entregou as 4 correções (worker Deno com import map real, migration 0017 cron+pg_net+Vault, filtro `status='ativo'`/`deleted_at`, textos da spec). 2ª revisão independente (`spec-reviewer`, agentId ac4172dbe916da274, opus) confirmou achados 1-3 resolvidos com evidência ao vivo, mas achou 1 achado ALTO NOVO (poison-pill: item malformado nunca sai da fila e satura o batch — `InvalidQueueMessageError` não deletava) + achado 4 parcialmente corrigido (3 pontos de texto remanescentes) + 2 MÉDIO (tipo `router_worker_verify_token` faltando em `database.types.ts`, mascarado por `tsconfig.json` excluir `supabase/functions`) + 2 BAIXO (import map sem extensão `.ts` em 2 imports type-only, cast sem Zod do batch RPC). Corrigidos inline (poison-pill + tipo + os 3 textos + import map) e redeployados — `tenant-router-worker` v4 ACTIVE. `registry.json`/frontmatter: `built: true, reviewed: true`. Achados aceitos sem fix (baixo valor/escopo maior): tooling gap `deno check` no gate (ver Sync Requests), cast sem Zod do batch RPC (padrão pré-existente, risco baixo — dado gerado pela própria 0016).

## Bloqueios
- [2026-09-10] **Dispatch de workers registrados indisponível nesta sessão.** Os SOPs `@backend-engineer` etc. vivem em `Logos-Tech-master/.claude/agents/` (23 agents), mas a sessão não foi aberta com o monorepo como raiz — `subagent_type` registrado não carrega. Para `/logos parallel` real, reabrir o Claude Code com cwd = raiz do monorepo. Enquanto isso: build inline ou `general-purpose` + path do SOP (mais caro).
- [2026-09-10] **Retomada pós-hiato (~4 semanas).** Working tree = estado real (wave 1 parcial). `git init` feito na pasta do projeto, remote `origin` → `github.com/ViniciusGrossi/Logos-Iris.git`, `core.longpaths=true`. `origin/main` congelado em `f3713cf` (13/08, wave 0 + frontend + fase 9 hardening — SEM a wave 1 backend). Commit local `76c184c` criado como baseline (1 ahead, **não pushado** — push nesse repo externo historicamente feito por Vinicius direto).
- [2026-08-13] ~~Repo `Logos-Iris` desincronizado~~ RESOLVIDO: Vinicius confirmou que `github.com/ViniciusGrossi/Logos-Iris.git` é o alvo de deploy (uso fora do que aparece no código, ex. Vercel via dashboard — sem CI/workflow/vercel.json no repo referenciando isso). Ressincronizado via commit `f3713cf` (branch `main` do repo aninhado, `--git-dir`/`--work-tree` explícitos pra não confundir com o monorepo) — push feito por Vinicius diretamente (bloqueado pro Claude pelo classifier de auto-mode mesmo após autorização em chat; push de remote externo não-`Logos-Tech` exige rodar fora da sessão ou ajuste de permissão em settings.json). Verificado: `main` local e `origin/main` idênticos em `f3713cf`, zero divergência. **Processo daqui pra frente:** não há automação ligando os dois repos — sessões de `/logos` que tocarem `Logos Iris/` devem sincronizar manualmente este repo aninhado após trabalho relevante (mesmo `git add -A` + commit + push nesse `.git`, escopado com `--git-dir`/`--work-tree` explícitos pra nunca depender de `cd`). O `.git` aninhado PERMANECE no disco (é o repo de deploy, não pode ser removido) — continua valendo a Lição abaixo sobre `isolation:"worktree"` cair nele por engano.
- [2026-08-11] ~~Schema `iris` fora de Exposed Schemas~~ RESOLVIDO: Vinicius adicionou `iris` em Settings → API → Exposed Schemas no projeto `nqubjiosnlaatxxamiut`.
- [2026-08-11] ~~Git uncommitted~~ RESOLVIDO: commit `e568d41` (12 arquivos, tenant-router-queue post-review corrections).
(bloqueio de 2026-07-15 resolvido em 2026-08-04: MCP Supabase reconectado, opção (a) do desbloqueio)

## Lições
> Claude registra aqui após cada correção do usuário neste projeto.
> Formato: [YYYY-MM-DD] erro cometido → regra derivada

- [2026-08-13] **Hipótese do cwd REFUTADA (parcialmente):** achei que o worktree cair no repo aninhado (`Logos-Iris`) era causado pelo cwd do orquestrador estar dentro de `Logos Iris/` no momento do dispatch — corrigi (cwd na raiz do monorepo antes de spawnar) e a rodada seguinte (persona-atendimento + knowledge-base-v1) funcionou corretamente. Mas na rodada seguinte, MESMO com cwd confirmado na raiz do monorepo, um worker (`contact-memory`) caiu no repo aninhado de novo. Conclusão: a causa raiz não é (só) o cwd — mecanismo do Agent tool pra escolher onde criar o worktree continua opaco/não-determinístico pra este projeto específico (provavelmente por causa do `.git` aninhado existir mesmo, não por causa de nenhuma ação do orquestrador). **Mitigação que FUNCIONA na prática:** todo brief de worker com `isolation:"worktree"` neste projeto deve INSTRUIR o worker a rodar `git remote -v` como primeiro passo e abortar com `STATUS: ❌` se o remote não for `Logos-Tech` — isso já pegou o problema em 16s/1 tool-call (vs. horas de recuperação manual da primeira vez). Regra: manter essa auto-checagem em TODO brief de worktree deste projeto indefinidamente (não é overhead descartável), e simplesmente redespachar quando acontecer — não investigar a causa de novo, o custo de um redespacho é baixo.
- [2026-08-13] `04-Projetos/01-Projetos/Logos Iris/` tem um `.git` ANINHADO (repo próprio, remote `Logos-Iris` no GitHub, história divergente desde `34b4907`) além do `.git` do monorepo. `isolation:"worktree"` do Agent tool resolve pro `.git` mais PRÓXIMO do cwd — se o worker opera dentro da pasta do projeto, ele pega o repo aninhado (stale) em vez do monorepo (onde tudo de fato é commitado/pushado), silenciosamente. Sintoma: worker relata "arquivo X não existe" pra arquivos que existem há sessões no monorepo. Regra: antes de qualquer `isolation:"worktree"` neste projeto, checar `git worktree list` DENTRO da pasta do projeto — se aparecer só uma entrada com branch tipo "main"/"master" e nenhuma relação óbvia com o commit mais recente do monorepo, é o repo aninhado. Recuperação (quando já aconteceu): comparar arquivo-base do worker (`git show <branch-base>:<path>` no worktree) contra o mesmo arquivo em `master` do monorepo — se idênticos, o diff do worker pode ser copiado direto; se master já tinha o arquivo (add/add), comparar as duas versões e decidir qual manter, nunca assumir que a mais nova é sempre a certa. Ver Bloqueios para a decisão pendente sobre o que fazer com o repo aninhado a longo prazo.
- [2026-08-04] `isolation: "worktree"` clona a partir do último **commit**, não do working tree — mudanças verificadas ao vivo mas nunca commitadas ficam invisíveis pra qualquer worker novo. Achado quando os 3 workers da wave 0 (whatsapp-gateway incluso) foram spawnados depois dos fixes da Fase 6 (migration 0014) terem sido aplicados e testados no DB real, mas nunca commitados no repo — os worktrees clonaram o estado pré-fix. Regra: fim de fase com mudança de schema/config aplicada ao vivo, `git commit` é parte do DoD da fase, não um "depois eu faço" — antes de qualquer `/logos parallel` que crie worktrees.
- [2026-08-04] Dois workers dispatchados em paralelo, ambos precisando de um client Supabase compartilhado (service-role), criaram arquivos `service-client.ts` divergentes de forma independente (add/add no merge) — um com schema selecionado por-query (`.schema(X).from(Y)`), outro com schema pinned na criação do client. Nenhum dos dois sabia que o outro existia (worktrees isolados). Regra: brief de worker que provavelmente precisa de infra compartilhada (client de banco, client HTTP) deve referenciar o arquivo esperado (path + convenção) pra reduzir a chance de reinvenção — se already existir um consumer do padrão A, o brief deve dizer "reusar A" em vez de deixar o worker decidir sozinho.
- [2026-08-04] Playbook fase-07-backend.md prescreve `/review-against-spec` como passo pós-merge — mas essa skill tem `disable-model-invocation` (só usuário digita, `Skill` tool recusa chamada minha). Mesma classe dos skill-bugs de fase-01/02/05 (to-prd, gsd-*), já registrados acima. Desviei: usei o subagent `spec-reviewer` (mesmo propósito — drift spec vs código) no lugar. Corrigir playbook fora deste projeto: trocar `/review-against-spec` por dispatch de `spec-reviewer`.
- [2026-08-04] Worktree de worker não roda `npm install` no repo principal — `package.json`/`package-lock.json` mergeiam limpo mas `node_modules` do working dir principal fica sem os deps novos (vitest, zod, @supabase/*) até `npm install` manual. Regra: sempre `npm install` antes de re-rodar suite pós-merge de feature backend, senão o "re-run" silenciosamente falha (exit 1, zero output) e parece erro de config.

- [2026-08-07] **INCIDENTE DE SEGURANÇA (remediado).** Worker `backend-engineer` (abf12184e67e03fa7), depurando um 401, fez deploy de uma versão de DEBUG da `tenant-router-worker` que ecoava o header `Authorization` completo (incl. o token dedicado do Vault) de volta na resposta HTTP, e consultou essa resposta via SQL — gravando o secret em texto puro em `net._http_response` (banco compartilhado com ~8 outros produtos Logos Tech). Não estava autorizado no brief. Detectado pelo classificador de segurança do harness, reportado a Vinicius ANTES de qualquer ação — Vinicius autorizou remediação completa. Remediação (orquestrador, não o worker): (1) confirmado que a versão ATIVA já não era mais a versão de debug; (2) apagadas as 5 linhas de `net._http_response` com o secret; (3) confirmado que logs de Edge Function (nível plataforma) não retêm corpo de resposta, só metadados — sem vazamento ali; (4) token dedicado rotacionado no Vault (`vault.update_secret`) — como o cron job busca o secret dinamicamente via `iris_private.tenant_router_worker_token()` a cada invocação (não baked-in), a rotação valeu no minuto seguinte sem tocar a migration. Regra: brief de worker que testa auth/token NUNCA deve pedir pra "ecoar" ou "logar" um header de autenticação pra debug, nem que pareça inofensivo num ambiente de teste — banco de dev/staging aqui É produção compartilhada. Regra para o orquestrador: ao receber um `SECURITY WARNING` de um report de worker, tratar como STOP incondicional — não prosseguir com merge/próximo passo até o usuário decidir a remediação.
- [2026-08-07] Suite verde (`tsc --noEmit`) não prova cobertura de tipos quando `tsconfig.json` exclui o diretório tocado (`exclude: ["supabase/functions"]`) — um erro de tipo real (`db.rpc("router_worker_verify_token", …)` sem entrada em `database.types.ts`) passou batido em 2 rodadas de `tsc --noEmit` porque as Edge Functions nunca entram no type-check do Next. Só apareceu quando o 2º `spec-reviewer` rodou `tsc` com um tsconfig temporário incluindo `supabase/functions`. Regra: `tsc --noEmit` limpo só é evidência de "zero erro" para os arquivos DENTRO do `include`/fora do `exclude` — sempre conferir se os arquivos da mudança atual realmente caem no escopo do comando antes de citar isso como prova no report.
- [2026-08-07] Poison-pill em consumidor de fila: um `catch` genérico que nunca deleta a mensagem da fila trata "vai falhar pra sempre" (payload malformado, `InvalidQueueMessageError`) igual a "falhou agora, tenta de novo" (erro de rede/DB transitório) — o item permanente reaparece a cada `vt` pra sempre e, se `BATCH_SIZE` mensagens malformadas se acumularem, elas saturam todo lote e travam a fila pra mensagens novas atrás delas, silenciosamente (endpoint ainda responde `200 {ok:true}`). Regra: todo consumidor de fila com retry-by-visibility-timeout precisa distinguir explicitamente erro permanente (deletar) de erro transitório (não deletar) no `catch` — nunca um `catch` só, sempre checar o tipo do erro.
- [2026-08-07] `git worktree add` do `/logos parallel` falhou com "Filename too long" — não é bug do worker/brief, é limite MAX_PATH (260 char) do Windows contra assets de path longo em `03-Design/tools/.../aura.build/assets/` (fora de Logos Iris, resto do monorepo). Worktree precisa checkout do repo INTEIRO, não só da subpasta do projeto. Fix: `git config core.longpaths true` (config local, não destrutiva, não afeta histórico) resolve — aplicado nesta sessão. Regra: se `/logos parallel`/dispatch de worker com `isolation:"worktree"` falhar com esse erro, checar `git config --get core.longpaths` antes de assumir que é problema do brief.
- [2026-08-04] pgTAP rodando como owner (`postgres`) mascara ausência de GRANT pra `service_role` — `BYPASSRLS` faz a policy RLS não bloquear, mas não supre a falta de `GRANT` de tabela; o caminho de produção real (webhook/engine autenticando como `service_role`) só quebra em runtime, não no teste. Regra: toda suite pgTAP de schema multi-role precisa de pelo menos 1 teste rodando `set local role service_role` (ou o role de menor privilégio real da aplicação), não só `authenticated`/owner.
- [2026-08-04] Worker de execução (dba aplicando migrations já escritas) que precisa reconstruir SQL de memória em vez de ler o arquivo literal introduz divergência (ver 0012: colunas erradas na 1ª tentativa). Regra: worker executor deve sempre citar o Read do arquivo antes de montar o `query` do `apply_migration` — nunca reconstruir de memória mesmo quando "óbvio".
- [2026-07-03] Fase 1 inicia com planejamento-fable.md existente → usar como insumo bruto do grill-me, não como decisão final; toda seção marcada [DECISÃO Vinicius] é locked-in, outras estão sob interrogação
- [2026-07-08] Playbook fase-01-ideia.md prescreve "/to-prd em modo captura" pra gerar docs/ideia.md — mas a skill to-prd real é PRD completo (User Stories/Implementation Decisions) + publish em issue tracker, sem "modo captura" leve. Desviei: escrevi docs/ideia.md inline no formato do gate. Skill-bug a corrigir no playbook, fora do escopo deste projeto.
- [2026-07-08] Playbook fase-02-prd.md prescreve `/gsd-discuss-phase` e `/gsd-spec-phase` como se fossem genéricos sobre docs/PRD.md — mas GSD real exige phase numérica + PROJECT.md/ROADMAP.md/STATE.md próprios (state machine paralela ao /logos, incompatível). Projeto só tem STATE-PROJECT.md do /logos. Desviei: rodei "discuss" e "spec" manualmente, sem a máquina GSD. Mesma classe de skill-bug do to-prd — corrigir nos dois playbooks fora deste projeto.
- [2026-07-11] Playbook fase-05-decomposition.md prescreve `/to-issue` e `/gsd-plan-phase` — mesma classe de skill-bug já registrada em fase-01/02: `to-issues` real publica em issue tracker (GitHub etc, inexistente aqui) e `gsd-plan-phase` exige máquina paralela (PROJECT.md/ROADMAP.md/STATE.md) incompatível com STATE-PROJECT.md do /logos. Desviei: tracer bullets + waves feitos manualmente, direto em docs/tasks.md. 3 playbooks (01, 02, 05) já pisaram nisso — considerar reescrever esses passos como "decompor manualmente em tracer bullets" em vez de invocar skill externa.
- [2026-07-11] O Skill tool resolveu `/logos` para o path user-global (`~/.claude/skills/logos`, v7 — cita `logos-cli/cli.py`, morto) mesmo com regra travada abaixo mandando v8 project-scoped. Invocação por nome sem prefixo de diretório não prioriza skill de projeto. Contorno: ignorei o output do v7 e segui a validação manual contra `.claude/skills/logos/` (v8) na raiz do repo. Regra pra próxima sessão: se possível invocar via prefixo de diretório; senão, sempre conferir se o SKILL.md carregado bate com o path v8 antes de agir sobre o conteúdo.
- [2026-07-15] Não existe projeto Supabase dedicado ao Logos Iris — instância compartilhada (`nqubjiosnlaatxxamiut`, "Teste de projetos") com 1 schema Postgres por produto (`delphi`, `logos_platform`, `logos_polis`, `concurso`, `padaria`, `paideia`, `Mens-Sana`, `Logus_Tech_Oficinas`). dba decidiu seguir o padrão: schema `iris` (tabelas) + `iris_private` (funções pgcrypto/SECURITY DEFINER) em vez do `private` genérico do ARCHITECTURE.md — evita colisão de nome entre produtos na mesma instância. Repassar essa convenção pro worker quando respawnar.
- [2026-07-08] RESOLVIDO: gate f2 `validate` apontava pra `logos-cli/logos/cli.py` (removido, __pycache__ vazio) — Vinicius consertou migrando pro `/logos` v8 self-contido: skill v8 vive em `.claude/skills/logos/` (raiz do repo, project-scoped) com `scripts/validate.py` stdlib-only, gate referencia `[SKILL_ROOT]/scripts/validate.py`. `~/.claude/skills/logos/` (user-global) continua v7, desatualizada. Regra do Vinicius: **sempre usar o v8** — daqui pra frente, ler playbooks/gates a partir de `C:\Users\everex\Documents\Logos Tech\.claude\skills\logos\`, não do path user-global.
