# STATE-HISTORY — Logos Iris

> Excedente do STATE-PROJECT.md (hard limit ~200 linhas). Só lido sob pedido explícito ou na fase 11.
> Append no topo, com data. Nunca deletar.

## [2026-09-25] Fase 7 — Em Andamento movido para cá (fase FECHADA, ver Concluído no STATE-PROJECT.md)

- [x] Fase 7: Build Backend — wave 1 (8 features, docs/tasks.md linhas 34-56). Grafo de dependência: `persona-atendimento` (7.1-7.2, sem dep de wave 1) bloqueia `persona-agendamento` (8.1-8.3, dep 7.1) e `human-handoff` (11.1-11.3, dep 7.1). `knowledge-base-v1` (9.1-9.2), `contact-memory` (10.1-10.2), `feature-gating-v1` (13.1), `cost-observability-v1` (14.1) são independentes entre si e de `persona-atendimento`. `painel-cliente-v1` (12.1-12.3) é FASE 8 (frontend), não fase 7, e sua ação 12.3 depende de `human-handoff` — fica pra depois.
  - Rodada 1: ✅ COMPLETO. `persona-atendimento` (worker a5da98badf747eb35 — 14/14 testes, migration 0020 wrapper SECURITY DEFINER) + `knowledge-base-v1` (worker a7ad2378cb00a24f4 — 30/30 testes). **Colisão de migration:** os 2 workers pegaram `0020` em paralelo sem se ver — knowledge-base-v1 renumerada pra `0021` antes do merge. **Achado do orquestrador pré-merge em knowledge-base-v1:** `SupabaseKnowledgeEntryRepository.setStatus` fazia UPDATE só por `id`, sem `.eq("tenant_id",...)` (inconsistente com `upsertDraft` e com o princípio de defesa em profundidade documentado no próprio arquivo) — não explorável hoje (todo `id` chega de `findByTenant` já tenant-scoped) mas corrigido antes do merge (parâmetro `tenantId` obrigatório + teste de spy dedicado). Merge `--no-ff` limpo nos 2. Pós-merge: `tsc --noEmit` limpo, `npm run build` limpo, suite 156/160 (mesmas 4 falhas pré-existentes). `registry.json`/frontmatter: ambos `built: true, reviewed: true`.
  - Rodada 2: ✅ COMPLETO (ambas caíram no bug do worktree/repo aninhado na 1ª tentativa, abortaram com segurança via auto-checagem, redespachadas). `feature-gating-v1` (worker a972c0ca288991ce4 — 30/30 testes, `CheckFeatureGate` default-deny, admin bypass ADR-030, `FollowUpService` como referência, sem migration nova). `contact-memory` (worker adae536591d40ab00 — 10/10 testes + pgTAP 07 novo, migration 0022: geração de resumo + purge diário LGPD/ADR-026 + cascata de esquecimento em `contacts.deleted_at` + fila pgmq + Edge Function `contact-memory-summarizer-worker`, reusa token Vault de 0017/0019 sem nunca ecoar/logar; estende `contact-memory.repository.ts` do persona-atendimento sem duplicar). 2 Sync Requests não-bloqueantes (task_type dedicado de sumarização no model_registry; tier_do_plano real no generator — ambas hardcoded como stopgap documentado). Merge `--no-ff` limpo nos 2. Pós-merge: `tsc --noEmit` limpo, suite 196/205 (mesmas 4 falhas pré-existentes). `registry.json`/frontmatter: ambos `built: true, reviewed: true`.
  - Rodada 3 (2026-08-13): `cost-observability-v1` + `persona-agendamento` despachada em paralelo — **nunca aterrissou** (sessão morreu; registry confirmou `built: false` na retomada de 2026-09-10).
  - Retomada 2026-09-10 (build INLINE, sem worker): `human-handoff` ✅ + `cost-observability-v1` ✅ COMPLETOS. `persona-agendamento` ficou pendente (dep. migration `tenants.timezone` + ALTO-2 Vault) — concluída na sessão de 2026-09-25 (ver STATE-PROJECT.md Concluído).
- [x] Fase 7: Build Backend — wave 0. `/logos parallel` dispatchado 2026-08-04.
  - `model-gateway-v1`: ✅ COMPLETO. Worker `backend-engineer` (16/16 testes) → merge `--no-ff` em master (sem conflito) → `npm install` + suite re-rodada 16/16 verde → `spec-reviewer` (agentId a8bc8d284cb7f4c65) auditou independente (rodou testes e `tsc --noEmit` de novo, não confiou no self-report): **"Merge liberado"**, zero drift spec/contrato/implementação, camadas C→S→R respeitadas, zero `any`. Único gap não-bloqueante: `ModelRegistryQueryError` (falha de driver, 502) sem teste direto — aceito. `registry.json`: `built: true`, `reviewed: true`.
  - `whatsapp-gateway`: ✅ BUILD completo, review pendente. Worker `backend-engineer` (agentId a00939d40c4f08cd3) entregou webhook receiver + Evolution/Cloud API/OpenWA adapters + dedup via `webhook_inbox`/pgmq. **Descoberta crítica pré-merge:** as correções da Fase 6 (migration 0014 — grants service_role, índices FK, RLS soft-delete, updated_at triggers) tinham sido só verificadas ao vivo no DB, nunca commitadas — `isolation: "worktree"` clona a partir do último commit, não do working tree, então o worktree do whatsapp-gateway (e os outros 2 da wave) nunca viu esses fixes. Commitado antes do merge (789b8bb). Merge `--no-ff` (1e25559) com 3 conflitos reais: `package.json` (mantido HEAD), `package-lock.json` (mantido HEAD), `service-client.ts` (add/add, resolvido preservando as duas funções). `01_rls_tenant_isolation.sql` mergeou limpo. Corrigido `any` em `IrisSupabaseClient` gerando `database.types.ts` real via Supabase CLI. Pós-merge: `npm install` limpo, `npm run test` 34/34 + 1 todo, `tsc --noEmit` sem erros. Suite pgTAP `01_rls_tenant_isolation.sql` re-rodada ao vivo — **15/15 `ok`**. `spec-reviewer` (agentId a77e6f35b141474e8) auditou independente: **"Merge liberado."** 3 achados não-bloqueantes registrados em Sync Requests (código morto `updateSessionStatus`, webhook secret fail-open + vars fora do `.env.example`, `HumanHandoffService` não chamado). `registry.json`/frontmatter: `built: true`, `reviewed: true`.
  - `conversation-engine-v1`: ✅ COMPLETO (build+review). Worker entregou só `compilePrompt` (camadas 1-2, ADR-027) — SEM Controller/route.ts e SEM consumir `model-gateway.service.ts`, spec-driven (Fora de Escopo). `spec-reviewer` (a52d9c78881de9af3) confirmou linha a linha. Verificação independente: 7/7 testes, `tsc`/`eslint` limpos, zero `any`, zero PII no prompt. **Merge liberado.**
  - `tenant-router-queue`: ✅ BUILD completo, ⚠️ REVIEW com blockers corrigidos depois (ver Concluído 2026-08-07 no STATE-PROJECT.md). Redispatch (agentId a245b153a49941b28) entregou 3 funções SECURITY DEFINER em `iris` (0016), `TenantRouterService`/factory/errors, repos, Edge Function `tenant-router-worker`, pgTAP 05. Merge `--no-ff` limpo. Verificação independente: `tsc --noEmit` zero erros · `vitest run` 44/44 · migration 0016 aplicada ao DB live + pgTAP 05 re-rodada — 9/9 `ok`. `spec-reviewer` (agentId aaff386f77f88e4f5) achou 2 CRÍTICOS + 1 ALTO (ver Concluído 2026-08-07), corrigidos.
  - Housekeeping pendente (2026-08-04): 3 worktrees de workers já mergeados (a00939d40c4f08cd3, a3302aec392112a21, af0691d8178d52f54) ainda em `.claude/worktrees/` — todos confirmados ancestrais de master, seguros pra `git worktree remove`. Prune bloqueado pelo classifier de auto-mode; limpar manualmente ou em sessão interativa. **Ainda não verificado se foi limpo.**
  - Nota de cobertura (não bloqueante): `ModelRegistryQueryError` em `model-registry.repository.ts` sem teste direto — mockar client Supabase pra cobertura 4/4 se algum dia doer.
